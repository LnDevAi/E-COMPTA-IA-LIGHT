spring:
  application:
    name: e-compta-ia-light
  
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  
  h2:
    console:
      enabled: true
      path: /h2-console
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

server:
  port: 8080

jwt:
  secret: mon-secret-jwt-tres-long-et-super-securise-pour-jwt-hmac-sha-256-bits-minimum
  expiration: 86400000
