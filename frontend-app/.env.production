# Production environment configuration
# When REACT_APP_API_URL is not set, the app will use relative URLs (same origin)
# This is the desired behavior when frontend is served from the backend
REACT_APP_API_URL=
